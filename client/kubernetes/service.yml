apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: mern-recipe-app
spec:
  selector:
    app: client
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 32000
